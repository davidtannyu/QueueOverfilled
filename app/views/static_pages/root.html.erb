<script type="text/javascript">
  <% if logged_in? %>
  window.currentUser =
    <%= render(
    'api/users/user.json.jbuilder',
    user: current_user).html_safe %>
  <% end %>
  window.logo = "<%= asset_path("favicon.png") %>";
</script>

<main id="root"></main>
